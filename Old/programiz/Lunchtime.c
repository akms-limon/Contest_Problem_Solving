#include <stdio.h>

int main()
{
    int t, a;
    scanf("%d", &t);
    while(t--)
    {
    scanf("%d", &a);
    if(a>=1 && a=<4)
    printf("Yes\n");
    else
    printf("No\n");    
    }
    return 0;
}